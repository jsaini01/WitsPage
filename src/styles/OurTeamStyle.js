import styled from "styled-components";

export const OurTeamsDiv = styled.div`
  border: solid black;

  margin: 18.5vw 0 0 0;
`;

export const Headings = styled.div`
  text-align: center;
`;

export const SmallHeading = styled.p`
  color: rgba(130, 130, 130, 1);
  font-weight: 400;
  font-size: clamp(0.5rem, 1.4vw, 1.5rem);
`;

export const MainHeading = styled.h1`
  font-size: clamp(2rem, 3.7vw, 4.5rem);
  height: clamp(1.6rem, 3.7vw, 4.5rem);
  line-height: 6vw;
`;

export const BorderDiv = styled.div`
  background-color: rgba(249, 197, 28, 1);
  height: clamp(0.5rem, 1vw, 1.5rem);
  width: clamp(10rem, 18.5vw, 20rem);
  margin: auto;
`;

export const PeraHeading = styled.p`
  font-size: clamp(0.5rem, 1.4vw, 2rem);
  text-transform: capitalize;
  font-weight: 700;
  line-height: 6.5vw;
`;

// img

export const AllImages = styled.div`
  display: flex;
  justify-content: center;
  margin: 2.8vw 0;
  position: relative;
`;

export const BackImg = styled.img`
  width: calc(100% - 31.97%);
`;

export const Animator = styled.img`
  position: absolute;
  right: clamp(20rem, 42.5vw, 60rem);
  top: clamp(0.5rem, 0.6vw, 2rem);
  width: calc(100% - 90.7%);
`;

export const Ellipse = styled.img`
  position: absolute;
  right: clamp(14rem, 29.7vw, 60rem);
  top: clamp(2.3rem, 3.7vw, 10rem);
  width: calc(100% - 89.4%);
`;

export const JuliaCenter = styled.img`
  position: absolute;
  right: clamp(16.65rem, 33.8vw, 60rem);
  top: clamp(5.4rem, 11vw, 30rem);
  width: calc(100% - 72.1%);
`;

export const Employee200 = styled.img`
  position: absolute;
  right: clamp(10.8rem, 23.7vw, 60rem);
  top: clamp(7.3rem, 14.1vw, 20rem);
  width: calc(100% - 92.15%);
`;

export const Ellipse1934 = styled.img`
  position: absolute;
  right: clamp(6rem, 12.8vw, 20rem);
  top: clamp(11.36rem, 22.2vw, 30rem);
  width: calc(100% - 87.3%);
`;

export const Ellipse1935 = styled.img`
  position: absolute;
  right: clamp(11.5rem, 25vw, 30rem);
  top: clamp(16.7rem, 34.7vw, 40rem);
  width: calc(100% - 87.57%);
`;

export const UxDesign = styled.img`
  position: absolute;
  right: clamp(19.1rem, 42.8vw, 50rem);
  top: clamp(19.81rem, 41vw, 50rem);
  width: calc(100% - 90.9%);
`;

export const Ellipse1940 = styled.img`
  position: absolute;
  left: clamp(15rem, 31.9vw, 50rem);
  top: clamp(16.3rem, 35.45vw, 50rem);
  width: calc(100% - 88.6%);
`;

export const Ellipse1939 = styled.img`
  position: absolute;
  left: clamp(11.1rem, 22.9vw, 50rem);
  top: clamp(10.6rem, 22vw, 50rem);
  width: calc(100% - 86.95%);
`;

export const Ellipse1938 = styled.img`
  position: absolute;
  left: clamp(9.32rem, 19.2vw, 50rem);
  top: clamp(2rem, 5.3vw, 50rem);
  width: calc(100% - 86.02%);
`;
